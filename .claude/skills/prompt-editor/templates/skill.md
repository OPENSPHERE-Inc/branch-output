---
name: {skill-name}
description: {one-line role description; describe in what situation it should be invoked}
allowed-tools: {comma-separated allowed tools, e.g., Read, Write, Edit, Glob, Grep, Bash(ls:*)}
---

# {Title}

{One or two sentences defining the AI's role.}

## Input

The user specifies {kind of input}. If the argument is `$ARGUMENTS`, interpret it accordingly.

## Options

- `--{name}` (default {ON|OFF}) — {behavior}

## Step 1 — {step name}

{concrete steps}

## Step 2 — {step name}

{concrete steps}

## Step 3 — Report

{what to tell the user}
